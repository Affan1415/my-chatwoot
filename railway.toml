[build]
builder = "nixpacks"

[deploy]
startCommand = "bundle exec rails db:chatwoot_prepare && bundle exec rails server -p ${PORT:-3000} -e production"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
